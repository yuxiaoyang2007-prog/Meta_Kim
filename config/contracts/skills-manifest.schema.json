{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://meta-kim.dev/contracts/skills-manifest.schema.json",
  "title": "Meta_Kim Skills Manifest",
  "description": "Shared configuration for third-party skill repositories installed by Meta_Kim",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "skillOwner", "skills"],
  "properties": {
    "$schema": {
      "type": "string",
      "minLength": 1
    },
    "schemaVersion": {
      "type": "integer",
      "minimum": 1,
      "description": "Manifest schema version"
    },
    "skillOwner": {
      "type": "string",
      "minLength": 1,
      "description": "Default GitHub owner/organization for skill repositories (can be overridden via META_KIM_SKILL_OWNER env var)"
    },
    "skills": {
      "type": "array",
      "minItems": 1,
      "description": "List of third-party skill repositories to install",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "repo"],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[a-z0-9][a-z0-9._-]*$",
            "description": "Unique identifier for the skill (used as directory name)"
          },
          "repo": {
            "type": "string",
            "minLength": 1,
            "description": "Repository source as owner/repo, HTTPS URL, or trusted SSH URL. Can use ${skillOwner} placeholder."
          },
          "subdir": {
            "type": "string",
            "description": "Static subdirectory path where the skill is located"
          },
          "subdirTemplate": {
            "type": "string",
            "description": "Template for platform-specific subdirectory (use {platform} placeholder)"
          },
          "subdirMapping": {
            "type": "object",
            "description": "Platform-specific subdirectory mapping",
            "properties": {
              "win32": { "type": "string" },
              "darwin": { "type": "string" },
              "linux": { "type": "string" },
              "default": { "type": "string" }
            }
          },
          "installRoot": {
            "type": "string",
            "enum": ["skills", "plugins"],
            "description": "Primary deploy folder under each runtime home (default: skills). Rare: use plugins when the canonical tree must live under plugins/; then a skills/<id> symlink/junction is created for discovery. Prefer pluginHookCompat when upstream hooks expect plugins/ but the canonical tree should stay in skills/."
          },
          "pluginHookCompat": {
            "type": "boolean",
            "description": "When true: deploy to skills/<id>/ (default), then create plugins/<id> as symlink/junction to skills/<id> so upstream Stop hooks that default to ~/.{runtime}/plugins/{id}/ still resolve. Do not combine with installRoot: plugins."
          },
          "claudePlugin": {
            "type": "string",
            "description": "Claude Code marketplace plugin identifier if this skill is also an official plugin"
          },
          "codexPlugin": {
            "type": "string",
            "description": "Codex plugin marketplace identifier when the package should be installed through Codex's native plugin flow instead of a skills/ fallback"
          },
          "cursorPlugin": {
            "type": "string",
            "description": "Cursor plugin marketplace identifier when the package should be installed through Cursor's native plugin flow instead of a skills/ fallback"
          },
          "marketplace": {
            "type": "object",
            "additionalProperties": false,
            "required": ["id", "repository"],
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1
              },
              "repository": {
                "type": "string",
                "minLength": 1,
                "description": "Marketplace repository source; normalized by the shared config loader."
              }
            }
          },
          "versionSource": {
            "type": "object",
            "additionalProperties": false,
            "required": ["provider", "repository", "manifestPath"],
            "properties": {
              "provider": {
                "type": "string",
                "enum": ["github-contents"]
              },
              "repository": {
                "type": "string",
                "minLength": 1
              },
              "manifestPath": {
                "type": "string",
                "minLength": 1
              },
              "pluginName": {
                "type": "string",
                "minLength": 1
              }
            }
          },
          "capabilities": {
            "type": "array",
            "description": "Runtime-neutral capabilities provided by this skill.",
            "items": { "type": "string" },
            "uniqueItems": true
          },
          "platformSupport": {
            "type": "object",
            "description": "Per-runtime support status for the skill capability, distinct from install targets.",
            "additionalProperties": {
              "type": "object",
              "required": ["status"],
              "properties": {
                "status": {
                  "type": "string",
                  "enum": ["native", "adapter-required", "degraded", "unsupported"]
                },
                "events": {
                  "type": "array",
                  "items": { "type": "string" }
                },
                "adapter": { "type": "string" }
              }
            }
          },
          "fallback": {
            "type": "object",
            "description": "Per-runtime fallback or degraded behavior notes.",
            "additionalProperties": { "type": "string" }
          },
          "fallbackContentDir": {
            "type": "string",
            "minLength": 1,
            "description": "Repository subdirectory used as a non-native plugin fallback."
          },
          "defaultSelected": {
            "type": "boolean",
            "description": "If false, setup does not pre-select this repo in the dependency multi-select (default: true)"
          },
          "installMethod": {
            "type": "string",
            "enum": ["pluginMarketplace", "gitClone", "gitCloneInstallScript", "subdirExtraction", "manual", "upstreamCli"],
            "description": "How this skill is installed on target platforms. pluginMarketplace=Claude Code plugin marketplace; upstreamCli=delegate to the dependency's own installer for non-Claude native runtime adapters; gitClone=plain git clone; gitCloneInstallScript=git clone then run setup script; subdirExtraction=git clone with subdir extraction; manual=copy files manually per README"
          },
          "upstreamPackage": {
            "type": "string",
            "description": "npm package used when installMethod is upstreamCli"
          },
          "upstreamProfile": {
            "type": "string",
            "description": "profile passed to the upstream installer when installMethod is upstreamCli"
          },
          "legacyNames": {
            "type": "array",
            "description": "Previous directory names for this skill that should be removed when installing the current version. Used to clean up renamed skills (e.g. find-skills -> findskill).",
            "items": {
              "type": "string"
            }
          },
          "targets": {
            "type": "array",
            "description": "Which runtimes this skill applies to. If omitted, applies to all runtimes.",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "enum": [
                "claude",
                "codex",
                "openclaw",
                "cursor",
                "opencode",
                "qwen",
                "zed",
                "gemini",
                "codebuddy",
                "antigravity",
                "joycode",
                "qoder"
              ]
            }
          },
          "hookSubdirs": {
            "type": "object",
            "description": "Per-runtime subdirectories within the repo that contain hook files to deploy to the runtime's hooks/ directory.",
            "additionalProperties": {
              "type": "array",
              "items": { "type": "string" }
            }
          },
          "globalHookSubdirs": {
            "type": "object",
            "description": "Per-runtime hook directories under the runtime home.",
            "additionalProperties": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": { "type": "string", "minLength": 1 }
            }
          },
          "hookConfigFiles": {
            "type": "object",
            "description": "Per-runtime hook config files within the repo to deploy alongside hooks.",
            "additionalProperties": { "type": "string" }
          },
          "globalHookConfigFiles": {
            "type": "object",
            "description": "Per-runtime global hook configuration paths.",
            "additionalProperties": { "type": "string", "minLength": 1 }
          },
          "hookExtraFiles": {
            "type": "object",
            "description": "Per-runtime extra files to deploy alongside hooks (e.g. templates). Each entry is { src, dest } where dest is relative to runtimeHome.",
            "additionalProperties": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["src", "dest"],
                "properties": {
                  "src": { "type": "string", "description": "Source path within the repo" },
                  "dest": { "type": "string", "description": "Destination path relative to runtimeHome" }
                }
              }
            }
          },
          "hookSettingsMerge": {
            "type": "object",
            "description": "Per-runtime hook settings to merge into the runtime's settings.json. Registers a hook command pointing to the deployed hookFile.",
            "additionalProperties": {
              "type": "object",
              "required": ["event", "hookFile"],
              "properties": {
                "event": { "type": "string", "description": "Hook event name (e.g. UserPromptSubmit)" },
                "hookFile": { "type": "string", "description": "Filename of the deployed hook script (must exist in hooks/)" },
                "timeout": { "type": "integer", "description": "Hook timeout in milliseconds" }
              }
            }
          }
        }
      }
    }
  }
}
