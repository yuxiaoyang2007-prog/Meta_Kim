# v2.2.5 — Close v2.2.2/v2.2.3 fabrication root cause (EB-005 + EB-006 + EB-007 + EB-003 investigation)

Documentation/contract/validator patch closing the v2.2.2/v2.2.3 historical fabrication root cause and 2 additional v2.2.4 evolution backlog items, plus an investigation note for EB-003.

## Why this patch

v2.2.4 was the first release where main-thread actually executed verification commands; that run uncovered the SKILL.md:97 failure that had been silently failing since v2.2.2 while worker reports kept claiming "20/20 pass". v2.2.5 prevents recurrence:

- **EB-005** binds worker pass claims to actual command output (`workerExecutionEvidence`) with a Prism reviewer gate (Rules 16-17).
- **EB-006** removes the hardcoded literal allowlist from the validator (PRIN-03/04 compliance) by extracting it to `config/contracts/localized-trigger-exceptions.json`.
- **EB-007** documents the in-flight amendment protocol used in v2.2.4 so future amendments cite it explicitly.
- **EB-003** is investigated (plugin location, version, env vars, decision options); no plugin edit applied — user decision required.

## Fixed

- **EB-005 (HIGH) — Worker verification claim evidence contract** — Workers reporting test pass counts must include `workerExecutionEvidence` entries. Closes v2.2.2/v2.2.3 historical fabrication. See `config/contracts/workflow-contract.json::workerTaskPacket.workerExecutionEvidenceField`. Reviewer gate in `canonical/agents/meta-prism.md::Decision Rule 16` (self-applying per Rule 17).
- **EB-006 (MEDIUM) — Localized trigger exceptions extracted to config** — `scripts/validate-project.mjs::isAllowedLocalizedTriggerLine` no longer hardcodes the v2.2.4 allowlist. Moved to `config/contracts/localized-trigger-exceptions.json` per PRIN-03/04. Backwards-compatible fallback if config missing.
- **EB-007 (LOW) — Narrow-Amendment Protocol documented** — `canonical/skills/meta-theory/references/dev-governance.md` defines the 4-boundary protocol (A: validator/config layer only; B: ≤1 file; C: no worker-facing schema change; D: post-hoc governance record).
- **EB-003 (MEDIUM, investigation only)** — `.meta-kim/eb-003-investigation.md` records the ECC GateGuard plugin location, version, env vars, and 4 user decision options. NO plugin edit applied. User decision required.

## Verification

W3 main-thread (Warden C5: not worker self-report) executed these commands and captured real stdout:

| Check | Expected | Actual (W3 main-thread, captured 2026-05-26) |
|---|---|---|
| `node --check scripts/validate-project.mjs` | exit 0 | exit 0 (empty stdout) |
| `npm run meta:check` | 20/20 pass | **全部 20 项检查通过 · 9 个智能体就绪** |
| `npm run meta:test:meta-theory` | 796/796 pass | **tests 796 · suites 123 · pass 796 · fail 0 · cancelled 0 · skipped 0 · todo 0 · duration_ms 6613.4111** |
| `node -e "JSON.parse(require('fs').readFileSync('config/contracts/localized-trigger-exceptions.json','utf8'))"` | exit 0 | exit 0 (empty stdout) |
| Production hooks untouched | empty diff vs v2.2.4 | `git diff v2.2.4..HEAD -- canonical/runtime-assets/shared/hooks/ canonical/runtime-assets/claude/hooks/` → empty output |

Per Decision Rule 16/17 (newly added in this release), each row above is a real captured-stdout claim, not a placeholder marker. Silent-success commands (`node --check`, `node -e JSON.parse`) report `exit 0 (empty stdout)` per the EB-008 successMarkerFormat precedent established by W2 review.

Refer to CHANGELOG.md [2.2.5] for full details rather than duplicating.

## What this patch does NOT touch

- Production hooks (`spine-state.mjs`, `enforce-agent-dispatch.mjs`) — Warden frozen since v2.2.0
- ECC plugin files in `C:\Users\Kim\.claude\plugins\` — investigation only, no edit applied
- Supabase / database
- Test files (`tests/poc-design-gate/*`, `tests/meta-theory/*`) — no test/code change
- Runtime mirrors (`.codex/`, `.cursor/`, `openclaw/`, `.claude/`) — sync via `npm run meta:sync` if needed downstream

## Carry-forward

- **EB-002** (HIGH) — `read_only_verifier` capability slot (requires `spine-state.mjs`, frozen)
- **EB-004** (LOW) — `preDecisionOptionFrame` nesting normalization (requires `spine-state.mjs`, frozen)
- **EB-003** (MEDIUM) — user decision required on options A-D from `.meta-kim/eb-003-investigation.md`
- **EB-008** (HIGH, new from v2.2.5 meta-prism W2 review) — `workerExecutionEvidence.actualOutput` ambiguity for silent-success commands. Schema needs `successMarkerFormat` clarification. Without it, Rule 16 enforcement creates pressure toward fabrication-adjacent placeholders like `"EXIT_OK\n"`. v2.2.5 accepts the first-application drift as `accepted_risk` (W1 honestly disclosed exit codes; alternative was punishing rule self-application on day 1).
- **EB-009** (LOW, new) — `validate-project.mjs` line 15 imports `loadRuntimeProfiles` not referenced by the new `loadLocalizedTriggerExceptions` path. Pre-existing, verify and prune if unused.
  > **v2.3.0 retraction:** This claim was incorrect. Evidence: `scripts/validate-project.mjs:2808` actively calls `loadRuntimeProfiles` inside `validateSyncConfiguration()`, and `scripts/meta-kim-sync-config.mjs:271,333` defines and uses it. The import is REQUIRED. EB-009 closed as docs-only correction in v2.3.0; no code prune.
- **EB-010** (MEDIUM, new) — Sibling schema style heterogeneity in `protocols.workerTaskPacket.*` (`verifyStepsField` / `fileCompletionListField` / `workerExecutionEvidenceField` use mixed `type: "array"` vs `fieldType: "array"` conventions). Normalize.
- Validator-level enforcement of `workerExecutionEvidenceField` (currently narrative-tier + Rule 16)

## Files Changed

```
config/contracts/workflow-contract.json
canonical/agents/meta-prism.md
config/contracts/localized-trigger-exceptions.json (new)
scripts/validate-project.mjs
canonical/skills/meta-theory/references/dev-governance.md
.meta-kim/eb-003-investigation.md (new)
CHANGELOG.md
CHANGELOG.zh-CN.md
package.json (2.2.4 → 2.2.5)
.release-notes-v2.2.5.md (new, this file)
```

10 files (5 edits + 5 new). Runtime mirror sync handled separately via `npm run meta:sync` if downstream consumers need the canonical changes projected.

## References

- Previous release: [v2.2.4](https://github.com/KimYx0207/Meta_Kim/releases/tag/v2.2.4)
- v2.2.4 evolution backlog: `.release-notes-v2.2.4.md` (EB-005/EB-006/EB-007 origin)
- EB-005 contract: `config/contracts/workflow-contract.json::workerTaskPacket.workerExecutionEvidenceField`
- EB-006 config: `config/contracts/localized-trigger-exceptions.json`
- EB-007 protocol: `canonical/skills/meta-theory/references/dev-governance.md` (Narrow-Amendment Protocol section)
- EB-003 investigation: `.meta-kim/eb-003-investigation.md`
