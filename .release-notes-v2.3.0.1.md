# v2.3.0.1 — EB-011 cleanup patch (hook name + docstring alignment)

Patch release closing one v2.3.0 reviewer-flagged backlog item (EB-011) without behavioral change.

## Why this patch

v2.3.0 shipped `canonical/runtime-assets/claude/hooks/ecc-batching-wrapper.mjs` as Option D for EB-003. The W2 review finding F1 flagged that the docstring overpromises ("batch the related ECC plugin install side-effects") while the implementation is a per-(session,file) permission auto-allow cache — closer to permission caching than true batching.

v2.3.0.1 closes F1 by aligning the file name and docstring with actual behavior.

## Changed

- Renamed: `canonical/runtime-assets/claude/hooks/ecc-batching-wrapper.mjs` → `canonical/runtime-assets/claude/hooks/ecc-permission-cache-wrapper.mjs` (`git mv`, history preserved)
- Docstring: aligned with actual permission-cache behavior (SHA256(session_id+file_path), 5min TTL, os.tmpdir())
- References updated: `.release-notes-v2.3.0.md`, `CHANGELOG.md`, `CHANGELOG.zh-CN.md`
- Version: 2.3.0 → 2.3.0.1
- W2 finding F1: closed

## No behavioral change

Cache key generation, TTL constant, path detection regex, and hook integration are unchanged. Pure naming/docs alignment patch.

## Verification

- `npm run meta:check` 20/20 pass
- `git log --follow --diff-filter=R -- canonical/runtime-assets/claude/hooks/ecc-permission-cache-wrapper.mjs` shows the rename event

## Carry-forward to v2.3.1

EB-002 (HIGH) + HOOK-INFRA-001 (LOW) + EB-004 (LOW) — bundled together; v2.3.1 cycle in progress.
